#include "main.h"
#include <stdio.h>

/**
 * mul - this function will multiply the user input
 * @a: the input to multiply
 * @b: the other input to multiply
 *
 * Return: the return type is an integer
 */

int mul(int a, int b)
{
	return (a * b);
}
